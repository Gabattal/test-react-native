import {DefaultTheme} from "@react-navigation/native";

export  const DarkTheme = {
    ...DefaultTheme,
    colors: {
        ...DefaultTheme.colors,
        background: '#272b33',
        color: '#fff',
    },
};